import { HighlightDirectiv } from './highlight.directive';

describe('HighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDirectiv();
    expect(directive).toBeTruthy();
  });
});
